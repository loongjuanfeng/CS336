# TODO：memory_block_residuals.png

所属题：`memory_profiling`。(f)证据：单个Block forward保存的residual，标出前五操作。

- [ ] 先完成该题的真实运行，记录trace/pickle的路径与commit。
- [ ] 排除warmup/初始化，选择有代表性的正式step；不裁掉时间标尺或关键轨道。
- [ ] 图内或图注标清模型、batch/context、精度、设备/rank和单位。
- [ ] 选择能直接支撑该小问结论的区域，检查文字在PDF正常缩放时可读。
- [ ] 将真实图片保存为 `memory_block_residuals.png`，在writeup中插入图片和说明。
- [ ] 保留原始数据引用，不用示意图假冒profiler截图。

> 本文件是截图任务stub，不是图片。没有运行前不要生成空白PNG占位。
